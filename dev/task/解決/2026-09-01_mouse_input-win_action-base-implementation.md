# タスク: mouse_input.py / win_action.py の初期実装

- 作成日: 2026-09-01
- 完了日: 2026-09-01
- 状態: 解決

## フロー（順番に実施する）

1. [x] `SPEC.md` を見る。修正が必要なら修正する。
2. [x] `mouse_input.py` / `win_action.py` を実装する。
3. [x] `main.py` に、`MouseInput.update()` を叩き続けるループを書く。
   - ループのたびにマウス移動方向とボタン状態を取得する。
   - ボタン状態は `IDLE` と `HELD` を除いた2状態（`PRESSED`/`RELEASED`）のみ print する
     （ターミナルが埋もれるため）。
   - マウス移動方向は `(0, 0)` の場合は print しない。
   - `win_action.py` は `main.py` からは呼び出さない。
4. [x] `test/` にテストを書く。
   - **絶対に、通過させるためだけのテストコードを書かない**。品質を担保するために書く。
5. [x] テスト全件パスするまで実装を修正する。
6. [x] `main.py` のループ開始前・他の処理が入る前にテストを実行するようにする。
   - `test/` に `main.py` から呼び出す用のテストランナースクリプトを作る。
   - 毎回の実行時にテストが通過する状態が崩れていないか確認する。
   - テストが1件でも失敗したらエラーを吐いて起動を止める。
7. [x] `mouse_input.py` / `win_action.py` の API リファレンスを `docs/api/` に書く。
   - 各関数について「どう使うか / 引数は何か / どういうときに使うか / 実際の書き方（サンプルコード）」
     を解決できるように書く。
   - 検索可能性のため、リファレンス項目には必ず1つ以上のタグを `<タグ名>` 形式で含める。
8. [x] `docs/INDEX.md` と `docs/TAG.md` を書く。
   - `TAG.md` にはタグの正本（一覧と説明）を書く。
   - `INDEX.md` は目次として、どのフォルダに何の情報があるかを示す
     （現状 `api/` のみだが、将来のフォルダ追加を見越した書き方にする）。
9. [x] 全ファイル（SPEC.md / 実装 / テスト / docs）に矛盾・誤り・整合性の欠如がないか確認する。
   - 問題があれば `ISSUE.md` に書き記す。
10. [x] すべて完了したら、このタスクファイルを `dev/task/解決/` に移動する。

## 完了メモ

- テスト: `python test/run_tests.py` で33件全件パス。
- 実装中に発見して修正したバグ: `MOUSEINPUT.mouseData` を符号なし(`c_ulong`)で
  定義していたため、`mouse_scroll` の負の値（下/左スクロール）が巨大な正の数に
  ラップされていた。`c_long`（符号付き）に修正。
- 最終整合性チェックで1件、方針要検討の issue を発見（実害なし）。
  詳細は [ISSUE.md](../../../ISSUE.md) および
  [dev/issue/未着手/2026-09-01_mouse_move-multi-monitor-and-clamping.md](../../issue/未着手/2026-09-01_mouse_move-multi-monitor-and-clamping.md)。
